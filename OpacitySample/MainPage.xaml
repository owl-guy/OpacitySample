<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Name="page"
             x:Class="OpacitySample.MainPage">
    <VerticalStackLayout>
        <BoxView BackgroundColor="Blue" HeightRequest="30" VerticalOptions="Start" Opacity="0.2"/>
        <Label TextColor="White" BackgroundColor="Black" Opacity="0.2" Text="Test"/>
        <Frame BackgroundColor="Green" Opacity="0.5"/>
        <Grid BackgroundColor="BurlyWood" Opacity="0.2" RowDefinitions="100" ColumnDefinitions="*">
            <BoxView BackgroundColor="Blue" HeightRequest="30"/>
        </Grid>
    </VerticalStackLayout>
</ContentPage>
